# 🚀 دليل التشغيل السريع - مديول المبيعات والمرتجعات

## 📋 المتطلبات الأساسية

### Backend API Endpoints المطلوبة

يجب أن يوفر الباك إند الـ Endpoints التالية:

```csharp
// 1. KPI Statistics
GET /api/Sales/kpi/today
Response: {
    totalSales: number,
    totalProfit: number,
    totalDebts: number,
    totalReturns: number,
    cashPercentage: number,
    creditPercentage: number,
    cashAmount: number,
    creditAmount: number
}

// 2. Sales Flow
GET /api/Sales/flow?days=7
Response: [
    { date: string, sales: number, profit: number, returns: number }
]

// 3. Payment Distribution
GET /api/Sales/payment-distribution?startDate=&endDate=
Response: [
    { method: string, amount: number, percentage: number, count: number }
]

// 4. Top Selling Products
GET /api/Sales/top-products?limit=10
Response: [
    { medicineName: string, quantity: number, revenue: number, profit: number }
]

// 5. Customer Debts Summary
GET /api/Sales/debts/summary
Response: { totalDebts: number, customersCount: number }

// 6. Returns Analysis
GET /api/Sales/returns/analysis?startDate=&endDate=
Response: { totalReturns: number, returnRate: number }
```

## 🎯 التشغيل

### 1. تأكد من تشغيل الباك إند
```bash
# في مجلد الباك إند
dotnet run
```

### 2. شغل الفرونت إند
```bash
# في مجلد الفرونت إند
npm install
ng serve
```

### 3. افتح المتصفح
```
http://localhost:4200/sales
```

## 🔧 الإعدادات

### Environment
تأكد من تحديث `environment.ts`:

```typescript
export const environment = {
    production: false,
    apiUrl: 'https://localhost:7000/api'  // عدّل حسب الباك إند
};
```

## 📊 الميزات المتاحة

### قائمة المبيعات
- ✅ عرض جميع الفواتير مع KPI Cards
- ✅ فلترة حسب الحالة والتاريخ
- ✅ مخططات حية (Area Chart + Donut Chart)
- ✅ أيقونات ديناميكية حسب الحالة
- ✅ مرتجع سريع من القائمة

### إنشاء مرتجع
- ✅ Mirror View (فاتورة أصلية + نموذج مرتجع)
- ✅ منع تجاوز الكمية المتاحة
- ✅ تحذيرات بصرية فورية
- ✅ حفظ كمسودة أو اعتماد مباشر

## 🎨 التصميم

### الألوان المستخدمة
- 🟢 `#28a745` - الأخضر الملكي (النجاح)
- 🔵 `#1e40af` - الكحلي العميق (البيانات)
- 🔴 `#dc2626` - الأحمر الفاخر (المرتجعات)
- 🟡 `#f59e0b` - الذهبي (التحذيرات)

### التأثيرات
- Glassmorphism
- Hover animations
- Pulse effects
- Glow effects
- Dark mode support

## 🐛 استكشاف الأخطاء

### المشكلة: البيانات لا تظهر
**الحل:** تأكد من:
1. الباك إند يعمل
2. CORS مفعّل في الباك إند
3. `apiUrl` صحيح في `environment.ts`

### المشكلة: خطأ في تحميل الفاتورة
**الحل:** تأكد من:
1. الفاتورة موجودة في قاعدة البيانات
2. الفاتورة معتمدة (Approved)
3. الفاتورة تحتوي على أصناف

### المشكلة: لا يمكن إرجاع الأصناف
**الحل:** تأكد من:
1. الكمية المرتجعة ≤ الكمية المتاحة
2. تاريخ المرتجع ≥ تاريخ الفاتورة
3. سبب الإرجاع مدخل

## 📝 ملاحظات مهمة

1. **البيانات الحية فقط:** لا توجد بيانات وهمية في النظام
2. **التحقق الصارم:** يتم منع تجاوز الكميات برمجياً وبصرياً
3. **الحالات الديناميكية:** الأيقونات تتغير حسب حالة الفاتورة
4. **المرتجع السريع:** يمكن إنشاء مرتجع مباشرة من قائمة المبيعات

## 🎯 الخطوات التالية

1. ✅ اختبر قائمة المبيعات
2. ✅ جرب إنشاء مرتجع جديد
3. ✅ تحقق من المخططات والإحصائيات
4. ✅ اختبر الأيقونات الديناميكية
5. ✅ جرب المرتجع السريع

---

**تم بناء هذا المديول بحب ❤️ وفخامة 👑**
